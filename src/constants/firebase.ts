export enum FirestoreCollections {
  Products = 'products',
  Stores = 'stores',
  Users = 'users',
  Categories = 'categories',
  Brands = 'brands',
  Ventas = 'ventas',
}

export enum UserRoles {
  Admin = 'admin',
  Employee = 'employee',
  Customer = 'customer',
}
