export enum FirebaseCollections {
    Products = 'products',
    Stores = 'stores',
    Users = 'users',
}

export enum UserRoles {
    Admin = 'admin',
    Employee = 'employee',
    Customer = 'customer',
}
