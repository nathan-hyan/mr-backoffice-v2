export enum FirestoreCollections {
  Products = 'products',
  Stores = 'stores',
  Users = 'users',
  Categories = 'categories',
  Brands = 'brands',
  Ventas = 'ventas',
  Providers = 'providers',
}

export enum UserRoles {
  Admin = 'admin',
  Employee = 'employee',
  Customer = 'customer',
}
